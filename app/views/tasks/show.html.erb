<h1>
  <%= @task.title %>
</h1>
<div>
  <%= @task.details %>
</div>
<p>
  <input type="checkbox" <%= @task.completed ? "checked" : false %>> <%= @task.completed ? "This task is completed" : "This task is not completed yet" %>
</p>

<%= link_to "Delete this task", task_path(@task.id), method: :delete %>
